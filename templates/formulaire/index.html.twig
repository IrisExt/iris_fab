{% extends 'base.html.twig' %}

{% block title %}Formulaires{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <table class="table table-striped table-middle-aligned">
        <thead>
        <tr>

        </tr>
        </thead>
        <tbody>
        {% for Appel in Appels %}
            <tr>

            <td>{{ Appel.getLbAppel() }}</td>

                <table class="table table-striped table-middle-aligned">
                    <thead>
                    <tr>
                        <th scope="col">Id Formulaire</th>
                        <th scope="col">Libellé</th>
                    </tr>
                    </thead>
                    <tbody>

    {% for formulaire in Appel.getTlFormulaireAppel() %}
    <tr>
        <td>{{ formulaire.getIdFormulaire() }}</td>
        <td><a href="{{ path('bloc', {tgAppelProj: Appel.getIdAppel(), tgFormulaire: formulaire.getIdFormulaire.getIdFormulaire, tgBloc: idBloc}) }}" class="btn btn-sm btn-default">{{ formulaire.getIdFormulaire.getLbFormulaire() }}</a></td>
    </tr>
    {% endfor %}

    </tr>
                    </tbody>
                </table>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
