{% trans_default_domain 'Personne' %}
<form method="post" action="{{ path('tg_personne_delete', {'idPersonne': tg_personne.idPersonne}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet personne?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tg_personne.idPersonne) }}">
    <button class="btn btn-danger"><span class="fa fa-trash-o"></span> {{ 'personne.from.delete'|trans }}</button>
</form>
