<form method="post" action="{{ path('tg_appel_proj_delete', {'idAppel': tg_appel_proj.idAppel}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet appel à projet?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tg_appel_proj.idAppel) }}">
    <button class="btn btn-danger"><span class="adminpro-icon adminpro-danger-error"></span> {{ 'action.delete'|trans }}</button>
</form>
